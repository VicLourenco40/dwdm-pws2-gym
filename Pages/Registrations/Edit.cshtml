@page
@model dwdm_pws2_gym.Pages.Registrations.EditModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Registration</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Registration.Id" />
            <div class="form-group">
                
                <label asp-for="Registration.StudentId" class="control-label"></label>
                <select asp-for="Registration.StudentId" class="form-control" asp-items="ViewBag.StudentId"></select>
                <span asp-validation-for="Registration.StudentId" class="text-danger"></span>
            </div>
            <div class="form-group">
                
                <label asp-for="Registration.SportId" class="control-label"></label>
                <select asp-for="Registration.SportId" class="form-control" asp-items="ViewBag.SportId"></select>
                <span asp-validation-for="Registration.SportId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
